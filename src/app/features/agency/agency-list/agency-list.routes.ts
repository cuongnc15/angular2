import { Routes } from '@angular/router';
import { AgencyListComponent } from './agency-list.component';

export const AGENCY_LIST_ROUTES: Routes = [
  { path: '', component: AgencyListComponent },
];
