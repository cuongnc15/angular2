import { Routes } from '@angular/router';
import { ServiceDetailComponent } from './service-detail.component';

export const SERVICE_DETAIL_ROUTES: Routes = [
  { path: '', component: ServiceDetailComponent },
];
