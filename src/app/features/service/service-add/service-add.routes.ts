import { Routes } from '@angular/router';
import { ServiceAddComponent } from './service-add.component';

export const SERVICE_ADD_ROUTES: Routes = [
  { path: '', component: ServiceAddComponent },
];
