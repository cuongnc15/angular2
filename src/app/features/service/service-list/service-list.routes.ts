import { Routes } from '@angular/router';
import { ServiceListComponent } from './service-list.component';

export const SERVICE_LIST_ROUTES: Routes = [
  { path: '', component: ServiceListComponent },
];
